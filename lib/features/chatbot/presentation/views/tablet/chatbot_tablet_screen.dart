import 'package:flutter/material.dart';
class ChatbotTabletScreen extends StatelessWidget {
  const ChatbotTabletScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
