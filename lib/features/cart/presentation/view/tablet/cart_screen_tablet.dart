import 'package:flutter/material.dart';
class CartScreenTablet extends StatelessWidget {
  const CartScreenTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
